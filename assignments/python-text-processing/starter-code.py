"""Starter code for the Python Text Processing assignment."""


def count_words(text):
    """Return the number of words in a string."""
    pass


def longest_word(text):
    """Return the longest word in a string."""
    pass


def clean_text(text):
    """Return a cleaned version of the text with extra whitespace removed."""
    pass


def process_file(input_path, output_path):
    """Read text from input_path, clean it, and write it to output_path."""
    pass


def write_word_report(input_path, output_path):
    """Read text from input_path, count word frequencies, and write a report."""
    pass


if __name__ == "__main__":
    sample = "Python makes text processing fun and practical."
    print("Word count:", count_words(sample))
    print("Longest word:", longest_word(sample))
    print("Cleaned text:", clean_text(sample))
